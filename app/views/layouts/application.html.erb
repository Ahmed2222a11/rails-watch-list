<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags 'movies'%>
  </head>

  <body>
  <nav class="navbar navbar-dark bg-dark">
  <div class="container d-flex justify-content-between align-items-center">
    <a class="navbar-brand">Save Movies</a>
    <div class="d-flex">
      <%= link_to "Create a New List", new_list_path, class: "btn btn-primary mx-2" %>
      <%= link_to "Create a New Movie", new_movie_path, class: "btn btn-primary mx-2" %>
    </div>
  </div>
</nav>
  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>
